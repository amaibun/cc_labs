`timescale 1ns / 1ps

module tb_demux1to8;

    reg EN;                // ???? ???????
    reg [2:0] A;          // ???? ????? (3-??????)
    wire [7:0] Y;         // ??????

    // ???????????? ???????????????
    demux1to8 uut (
        .EN(EN),
        .A(A),
        .Y(Y)
    );

    initial begin
        // ?????????????
        EN = 1; // ???????????? EN ? 1 (??????? ????????)
        A = 3'b000; // ????????? ???????? A

        // ?????? ????????
        #10;
        
        // ?????????? ? EN = 0
        EN = 0; // ?????????? ?????? ????????????????
        for (A = 0; A < 8; A = A + 1) begin
            #10; // ???????? ??? ????????????? ??????
        end

        // ?????????? ? EN = 1
        EN = 1; // ??????? ????????
        #10; // ???????? ??? ????????????? ??????

        // ?????????? ?????????
        $finish;
    end

    initial begin
        // ????? ??????? Y ? ???????
        $monitor("Time: %0t | EN: %b | A: %b | Y: %b", $time, EN, A, Y);
    end

endmodule

